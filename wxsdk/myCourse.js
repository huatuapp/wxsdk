const app = getApp(); import common from 'common.js'; const url = "wxapp/mycourse/course"; const params = []; const wxRequest = (params) => common.wxRequest(params, url); function myCourse(e) { var data = e; var userinfo = wx.getStorageSync('userinfo'); data.uid = userinfo.id; data.yg_uid = userinfo.yg_uid ? userinfo.yg_uid : ''; data.yg_token = userinfo.yg_token ? userinfo.yg_token : ''; data.mobile = userinfo.mobile; data.terminal_type = common.getTerminalType(); return new Promise(function (resolve, reject) { wxRequest({ data: data, method: "GET", success: (res) => { let result = res.data; resolve(result) } }) }) } module.exports = { myCourse }