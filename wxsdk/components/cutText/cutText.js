import common from '../../common.js'; Component({ properties: { cutText: { type: String, value: '' }, length: { type: Number, value: 0 }, ellipsis: { type: Boolean, value: false }, begin: { type: Number, value: 0 } }, data: {}, attached() { var cutText = this.data.cutText; var cutTextAfter = ''; var length = this.data.length; var ellipsis = this.data.ellipsis; var begin = this.data.begin; if (common.emptyFun(cutText)) { cutTextAfter = '' } else { if (length != 0) { cutTextAfter = cutText.substr(begin, length); if (ellipsis == true) cutTextAfter += '…' } else { cutTextAfter = cutText } } this.setData({ cutText: cutTextAfter }) }, })