const app = getApp(); import common from 'common.js'; const url = "wxapp/order/myorder"; const params = []; const wxRequest = (params) => common.wxRequest(params, url); function myOrder(e) { var data = e; var userinfo = wx.getStorageSync('userinfo'); data.uid = userinfo.id; return new Promise(function (resolve, reject) { wxRequest({ data: data, method: "GET", success: (res) => { let result = res.data; resolve(result) } }) }) } module.exports = { myOrder }